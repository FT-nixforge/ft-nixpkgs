# Auto-generated by scripts/gen-registry.py — do not edit manually.
# Source of truth: flakes/*/default.nix (meta block)
# Regenerate: python scripts/gen-registry.py

families:
  ft-nixpalette:
    parent: nixpalette
    children:
      - nixpalette-hyprland
    description: Theming and color management

flakes:
  nixpalette:
    name: ft-nixpalette
    type: library
    family: ft-nixpalette
    role: parent
    repo: github:FT-nixforge/nixpalette
    description: Base16 color theming engine
    provides:
      - nixosModules
      - homeModules
      - lib
    dependencies: []
    status: stable
    version: 1.0.0

  nixpalette-hyprland:
    name: ft-nixpalette-hyprland
    type: bundle
    family: ft-nixpalette
    role: child
    repo: github:FT-nixforge/nixpalette-hyprland
    description: Hyprland-specific theming bundle for ft-nixpalette
    provides:
      - nixosModules
      - homeModules
    dependencies:
      - nixpalette
    status: stable
    version: 1.0.0

  nixprism:
    name: ft-nixprism
    type: module
    family: null
    role: standalone
    repo: github:FT-nixforge/nixprism
    description: Rofi app launcher
    provides:
      - packages
      - homeModules
    dependencies:
      - nixpalette
    status: stable
    version: 1.0.0

# Planned flakes (not yet available)
# planned:
#   - nixui      # Shared UI component library
#   - nixbar     # Unified status bar
#   - nixlock    # Advanced lock screen
#   - nixnotify  # Notification daemon
#   - nixvault   # Password manager launcher
#   - nixcast    # Screen capture tool
#   - nixswitch  # Desktop profile switcher
#   - nixterm    # Unified terminal experience
#   - nixfont    # Font management system
#   - nixsync    # Config synchronization
#   - nixdev     # Project environment bootstrapper
